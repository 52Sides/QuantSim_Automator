План итераций (пошагово, без временных обещаний — вы решаете темп)

Итерация 0 — подготовка репозитория и окружения
что будет: git репозиторий, pyproject.toml/requirements.txt, структура папок (core/, cli/, services/, tests/).
почему: чистая структура облегчает масштабирование (см. ТЗ).

Итерация 1 — MVP CLI (локальная симуляция)
что будет: typer CLI (cli/run.py) с командой simulate:
использует core.portfolio.PortfolioSimulator для расчётов;
простой fetcher (core.fetchers.yfinance_fetcher) — возвращает котировки;
сохраняет result.json и печатает краткую метрику.
документация / обучение: пошаговый разбор кода, объяснение архитектурных решений, как протестировать локально.

Итерация 2 — тесты и CI
что будет: pytest тесты для метрик (CAGR, Sharpe, max drawdown), GitHub Actions для запуска тестов.

Итерация 3 — Web API (FastAPI)
что будет: backend/app/main.py с /simulate endpoint, который стартует задачу симуляции (синхронно в MVP).
обучение: разница между CLI и API, Pydantic-модели, dependency injection.

Итерация 4 — фоновые задачи (Celery) + Kafka (events)
что будет: вынесение симуляций в Celery worker, публикация событий в Kafka.
обучение: почему нужен Celery, как связать с FastAPI, схема событий (см. ТЗ).

Итерация 5 — frontend (React) + realtime
что будет: минимальная страница с формой симуляции и графиком (Plotly).
обучение: как подписаться на события через WebSocket/Kafka gateway.

Итерация 6 — экспорт отчётов, докеризация, mkdocs
что будет: html/xlsx экспорт, Dockerfile, docker-compose, mkdocs с документацией.


ИТЕРАЦИЯ 0
PortfolioSimulator — ответственен за бизнес-логику: расчёт доходностей и основных метрик (CAGR, Sharpe, Max Drawdown).
Методы отделены: daily_returns, cumulative_returns, cagr, sharpe_ratio — это облегчает тестирование и повторное использование.
Почему pandas.Series? В реальных задачах временные ряды удобны именно в pandas (датированные индексы, ресемплинг и т. д.).
Результат возвращается в SimulationResult dataclass — удобная контейнерная структура для сериализации/экспорта.

Почему именно такая структура:
Папка	Смысл
core	бизнес-логика и симуляции, независимая от интерфейсов
cli	интерфейс командной строки — точка входа
fetchers	модули для получения данных (API, Yahoo, CoinGecko и т.п.)
tests	модульные тесты, чтобы не сломать старую логику
requirements.txt	зафиксированные зависимости
README.md	документация, как запустить проект

Почему делаем это сразу:
С самого начала формируем чистую архитектуру;
Позволяет работать итеративно (CLI → API → Worker → UI);
Легко писать тесты, CI, и упаковывать в Docker.

-------------------------------
Итерация 1 — MVP CLI + Core (PortfolioSimulator).
Это первая «живая» версия программы: мы сможем запускать симуляции, считать метрики, и видеть результат в консоли и JSON-файле.

Цели Итерации 1
Реализовать ядро симуляции (PortfolioSimulator) — расчёт:
дневных доходностей,
накопленной доходности,
CAGR, Sharpe Ratio, Max Drawdown.
Добавить fetcher для загрузки данных (из Yahoo Finance или синтетический).
Написать CLI (через Typer), команду simulate.
Добавить минимальный тест.
Понять архитектуру и механику работы.

python -m cli.run AAPL --start 2020-01-01 --end 2021-01-01  # тут нет simulate из-за typer - если 1 команда, то ее вызывать не надо
pytest -v  # запуск мб с ошибкой (не знает где искать модули)
python -m pytest  # Запуск тестов из корня проекта (корректный)


Теперь у нас:
✅ рабочее ядро симулятора (PortfolioSimulator),
✅ загрузка данных с yfinance,
✅ CLI-интерфейс,
✅ успешные тесты,
✅ первый полноценный результат (result.json).

Ты фактически собрал MVP, готовый к обрастанию сервисами, API и CI.


Итерацию 2 — “Тесты и CI/CD”.
Цели Итерации 2
📦 Расширить тесты (tests/) — чтобы покрыть все основные метрики.
⚙️ Настроить GitHub Actions (файл .github/workflows/tests.yml), чтобы тесты запускались автоматически при пуше/PR.
🧠 Разобрать, как работает CI/CD на практике (автоматические проверки кода).


ci/cd:
🧠 Как это работает
Шаг				Что делает
on.push и on.pull_request	триггеры — тесты запускаются при пуше и PR
runs-on: ubuntu-latest		окружение, где GitHub создаёт виртуальную машину
actions/checkout		клонирует репозиторий
actions/setup-python		ставит Python
pip install -r requirements.txt	устанавливает зависимости
pytest -v			запускает тесты